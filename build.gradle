apply plugin: 'maven'
apply plugin: 'java'

repositories {  maven { url 'http://artifactory/development' }  }

dependencies { compile 'org.asciidoctor:asciidoctor-gradle-plugin:0.7.2'}

javadoc { options.encoding = "UTF-8" }