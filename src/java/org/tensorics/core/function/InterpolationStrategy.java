package org.tensorics.core.function;

import java.util.Map;
import java.util.SortedMap;

public interface InterpolationStrategy<X, Y> {

    // as an idea
    // Y interpolate(Map<X, Y> values, X xValueToInterpolate);

}
